# shellcheck shell=sh
# shellcheck disable=SC2034
# modes/diag.conf — declarative config for the diag mode.
# diag performs a pre-reboot EFISP-install state check and writes a state
# bundle to /sdcard/. Still zero device writes.

MODE_NAME="diag"
MODE_DESC="pre-reboot install-state diagnostic (no writes); writes a bundle to /sdcard/"
MODE_WRITES=""
MODE_TOOLS="gbl"
MODE_EFI=""
MODE_LIB=""
